<form method="post" action="{{ path('admin_fiche_personnage_lr_delete', {'id': fiche_personnage_lr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_personnage_lr.id) }}">
    <button class="btn">Delete</button>
</form>
