<form method="post" action="{{ path('admin_front_page_nouveau_perso_delete', {'id': front_page_nouveau_perso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ front_page_nouveau_perso.id) }}">
    <button class="btn">Supprimer</button>
</form>
