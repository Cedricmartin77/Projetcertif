<div class="container">
{{ form_start(form) }}
  <div class="text-light">
    <p>Email</p>
    {{ form_widget(form.email) }}
    <p>Pseudo</p>
    {{ form_widget(form.pseudo) }}
    <p>Password</p>
    {{ form_widget(form.password) }}
    <button class="btn5">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
</div>
